import { styled } from "styled-components";

export const ProfileBottomFolder = () => {
  return(
    <ProfileBottomFolderWrap>
    폴더 리스트가 보여요
    </ProfileBottomFolderWrap>
  )
}

const ProfileBottomFolderWrap = styled.section`
  
`